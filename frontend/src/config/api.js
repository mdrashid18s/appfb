/**
 * Central API configuration.
 * Empty string = relative URLs — Vite proxy forwards /api/* to Laravel backend.
 * No IP address needed. Works on laptop, mobile, any device on the network.
 */
export const API_BASE_URL = '';
